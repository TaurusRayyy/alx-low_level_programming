#include <stdlib.h>
#include <time.h>
/* main - Entry  */
/* Return: Always 0 */
int main(void)
{
	int n = 0;
	if(n > 0 );
	printf("n is positive/n"); 
}	
	if(n = 0 );
	printf("n is zero/n"); 
{	
	else if(n < 0 );
	printf("n is negative/n");
}
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	return (0);
}
