#include "main.h"
/**
* print_alphabet - print alphabet
*
* Return: Always 0
*/
void print_alphabet(void);
{
	int print_alphabet(int a)
	char a;

	for (a = 'a'; a <= 'z'; a++)
		_putchar(a);

	_putchar('\n');

	return (0);
}
